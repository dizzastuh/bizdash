apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bizdash.fullname" . }}-test-connection"
  labels:
{{ include "bizdash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bizdash.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
